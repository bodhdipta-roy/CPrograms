#include<stdio.h>
int main()
{
    double a,sq;
    printf("Input Number\n"); //taking input
    scanf("%lf",&a);
    sq=a*a; //squaring number
    printf("The square of the given number is = %lf\n",sq); //printing result
    return 0;
}
